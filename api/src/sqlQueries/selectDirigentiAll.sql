SELECT DISTINCT stag.nome stagione, sq.nome squadra, dir.nome, dir.cognome, dir.cf FROM dirigenti dir inner join d_tesserati dtess on dtess.id_giocatore = dir.id left join stagioni stag on stag.id = dtess.id_stagione left join squadre sq on sq.id = dir.id_squadra WHERE stag.id = 3 order by sq.nome;